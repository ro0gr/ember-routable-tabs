{{#each tabs as |tab|}}
  {{#if hasBlock}}
    {{#routable-tabs/tab-item tab=tab}}
      {{yield tab}}
    {{/routable-tabs/tab-item}}
  {{else}}
    {{routable-tabs/tab-item tab=tab}}
  {{/if}}
{{/each}}